# Phase 3 reuse audit

Source repository pin: `../retail_ai @ df1e707f1e4756ec1501420b3f9b5bc4f4143efe`.

| Source path @ pin | Source bytes | Destination | Grade | Changes on entry | Dropped and why | Tests now covering it | Reviewer | Date |
|---|---:|---|---|---|---|---|---|---|
| `features/build.py @ df1e707` (`sha256:0af4e7d6…`) | 24,269 | `ml/src/retail_ml/features/build.py` | REUSE + REDESIGN | `retail_v2`; verified bundle only; `SeriesKey` includes channel; h1…h26; active-assortment exposure; market-local calendar; dimensionless price signals; fixed embargo/availability metadata; atomic local publication | M5 fields and region/SNAP logic; `sku×store`; promo feature; raw price output; `reports.artifact_identity`; local manifest fingerprint; POSIX assumptions | `test_feature_schema.py`, `test_series_key_invariants.py`, `test_assortment_coverage.py`, `test_embargo.py`, `test_bundle_verification.py`, full W0.6 report | **PENDING independent review** | 2026-07-30 |
| `models/baselines.py @ df1e707` (`sha256:fd6ef38d…`) | 2,184 | `ml/src/retail_ml/models/baselines.py` | REUSE + EXTEND | Preserve naive, seasonal-naive, MA8 and MA13; exact additive metric components; explicit accuracy formula | Source-local persistence and non-additive presentation metrics | `test_model_primitives.py`, `test_forecast_run_publication.py` | **PENDING independent review** | 2026-07-30 |
| `models/backtest.py @ df1e707` (`sha256:fe4dcf31…`) | 4,747 | `ml/src/retail_ml/models/backtest.py` | REUSE + EXTEND | Fixed 26/2/13/104 schedule; global and per-market A1–A5; frozen slow-mover sufficiency and clustered interval | M5-only slices and publication coupling | `test_model_primitives.py`; full 13-origin h1 diagnostic | **PENDING independent review** | 2026-07-30 |
| `models/intermittent.py @ df1e707` (`sha256:e9a6bcce…`) | 6,767 | `ml/src/retail_ml/models/intermittent.py` | REUSE + CORRECT | Every history, routing and replay key is the shared channel-aware `SeriesKey`; linear historical Croston replay | Two-field `sku×store` keys and repeated quadratic prefix recomputation | `test_model_primitives.py`, `test_series_key_invariants.py`; full 13-origin h1 diagnostic | **PENDING independent review** | 2026-07-30 |
| `models/train_lgbm.py @ df1e707` (`sha256:36996be0…`) | 11,029 | `ml/src/retail_ml/models/train_lgbm.py` | REUSE + EXTEND | h1…h26; market categorical; governed per-market/global calibration; exact confidence; native LightGBM TreeSHAP grouped to five live drivers | Unfrozen deterministic blend explanation, promo driver, raw cross-currency price | `test_lgbm_model.py`, `test_driver_aggregation.py` | **PENDING independent review** | 2026-07-30 |
| `models/forecasting.py @ df1e707` (`sha256:8654a6ec…`) | 46,209 | `ml/src/retail_ml/models/forecasting.py` | REUSE + REDESIGN | Bounded portable thread scheduling; retrain every formal origin; MLflow evidence; immutable diagnostics; no accepted publication on failed gates | Source database/report publication, M5 paths, unsafe identity coupling and UI-oriented persistence | 13-origin h1 diagnostic (`17,414` rows) plus model and runtime tests | **PENDING independent review** | 2026-07-30 |

No `reports`, `pipeline`, `retail_ai`, ingestion, or datagen import crossed into `ml/`.

## Phase 4 engine foundation (P4-5)

| Source path @ pin | Source bytes | Destination | Grade | Changes on entry | Dropped and why | Tests now covering it | Reviewer | Date |
|---|---:|---|---|---|---|---|---|---|
| `engines/reorder.py @ df1e707` (`sha256:9ff4697b…`) | 9,410 | `ml/src/retail_ml/engines/primitives.py`, `engines/abc.py` | REUSE + RE-CONTRACT | Fractional-horizon sum/RSS ported unchanged (the load-bearing math); safety stock re-based on policy-v2 formula with canonical decimal service levels; ABC re-based to annualized consumption value in market-local minor units with `share_before_current_sku`, frozen tie-break and reason-coded missing cost; explicit disjoint ATP buckets | `service_level_scale`'s z(0.90) normalization (policy v2 uses raw z); `cap_order_to_cover`'s silent round-down-to-zero below MOQ (policy v2 freezes `refuse_with_reason`, so a conflict raises `MOQ_EXCEEDS_MAX_COVER`); store-only keys; `ReorderPolicy` dataclass (policy v2 resolver owns configuration) | `test_engine_foundation.py` (28) | **PENDING independent review** | 2026-08-01 |
| `engines/policy_calibration.py @ df1e707` (`sha256:0b6cfa95…`) | 6,438 | `ml/src/retail_ml/engines/cohorts.py` | REUSE + RE-CONTRACT | Deterministic sha256 cohort hash with versioned id `cohort-assignment/v1`, frozen seed 20260801, whole-key assignment over the five-field P4-D14 key | M5's store-only cohort key and source-local config loading | `test_engine_foundation.py` cohort suite | **PENDING independent review** | 2026-08-01 |
| `engines/policy_validation.py @ df1e707` (`sha256:67bbca01…`) | 4,977 | (holdout semantics recorded in `contracts/inventory/acceptance.schema.json`) | CONTRACT-ONLY | Read-once holdout, disjointness and calibration-cannot-compensate rules lifted into the acceptance schema where the verifier recomputes them | Runtime port deferred with `P4-7`: validation is scored against replay outputs that do not exist until the replay engine runs | schema tests in `contracts/python/tests` | **PENDING independent review** | 2026-08-01 |
| `engines/simulator.py @ df1e707` (`sha256:e42a4f4f…`) | 44,058 | `ml/src/retail_ml/engines/clock.py` (clock only) | REDESIGN — NOT PORTED | The daily/store-only simulator is not mechanically wrapped. The market-local ISO-Monday clock with the preceding-Thursday 23:00 bridge is implemented net-new under §3.5 with zoned local instants and DST fixtures | The entire daily event loop: weekly stock evidence produces a weekly replay, and interpolation is forbidden (invariant 6). The replay engine lands with `P4-7` against the v13 pin | `test_engine_foundation.py` clock suite incl. spring-forward | **PENDING independent review** | 2026-08-01 |
| `engines/replenishment_config.py @ df1e707` (`sha256:53a1e852…`), `engines/policy_config.py` (`sha256:df9e0b24…`) | 6,882 | (replaced by `retail_contracts.guardrails` v2 resolution) | REPLACED | Source-local config loading replaced by the version-aware guardrail resolver with market-override merge and fail-closed zero/duplicate/wrong-currency behavior | Everything: config files as policy authority are the defect policy v2 exists to fix | `contracts/python/tests/test_guardrails.py` (8) | **PENDING independent review** | 2026-08-01 |
| `engines/replenishment_inventory.py @ df1e707` (`sha256:bdb7a7ba…`) | 23,514 | `ml/src/retail_ml/engines/primitives.py` (position/ATP), remainder deferred | PARTIAL PORT | Inventory-position and ATP primitives with disjoint buckets and negative-inventory refusal | DoS/turn/health computation deferred to `P4-6` net-new engines against the v13 pin, where store-grain state exists to compute on | position tests in `test_engine_foundation.py` | **PENDING independent review** | 2026-08-01 |
| (no M5 source) | — | `ml/src/retail_ml/engines/resolution.py`, `engines/interval_guard.py` | NET-NEW | Lane resolution (§3.6) and supply-term precedence (§3.7) fail-closed with reason codes; decision-#92 interval guards: startup refusal for all-or-nothing consumers, per-row `interval_available` branching with the one-exception-per-series ledger and the P4-D17 market floors | — | resolution and interval suites in `test_engine_foundation.py` | **PENDING independent review** | 2026-08-01 |
