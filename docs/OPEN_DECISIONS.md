# Decision registry

Decisions #1–#83 are finalized; status is **DECIDED**. Implementation and evidence gates remain tracked separately in
`plans/local/tasks.md` and the applicable implementation plan. Spec refs are sections of
`demand_forecast_poc_spec.md`.

## Locked

| # | Decision | Status | Value |
|---|---|---|---|
| 1 | Monorepo name | **DECIDED** | `retail-intelligence` |
| 2 | Data production vs ingestion vs ML split | **DECIDED** | `datagen/` owns only its source spec and source-shaped publications; top-level `ingestion/` owns raw→Gate A→staging→transform→`retail_v2`→Gate B→curated; `ml/` consumes curated only |
| 3 | ML language / API language | **DECIDED** | Python ML pipelines, Go API |
| 4 | Money storage | **DECIDED** | integer minor units paired with `currency_code` (`INR` paise, `USD/EUR` cents, `GBP` pence); exact source reconciliation is per currency; tenant reporting conversion is derived with as-of FX |
| 5 | Contract version | **DECIDED** | `retail_v2` (superset of M5 `retail_v1`) |
| 10 | Forecast horizon | **DECIDED** | Weekly horizons `h1…h26`; the dashboard may select 13 or 26 weeks, but feature, target, availability and future-calendar schemas share the full declared 26-horizon set |
| 11 | Accuracy definition | **DECIDED** | `accuracy = 100·(1−WAPE)` where `WAPE = SUM(abs_error_sum)/SUM(actual_sum)`; aggregate additive terms before dividing, and report `insufficient_evidence` when `actual_sum = 0` |
| 16 | Fingerprint canonicalization spec | **DECIDED** | `semantic-fingerprint/v1`: exact-path volatile stripping via versioned RFC 6901 JSON Pointers → RFC 8785 JCS → SHA-256; floats/native decimals forbidden, JSON integers limited to ±(2^53−1), other numerics use canonical decimal strings; shared Python/Go vectors |
| 17 | UI framework | **DECIDED** | React + Vite + TypeScript + Tailwind, with TanStack Query/Table, Recharts and Zod; no SSR for the PoC. The Go API serves every number, and the static v6 mockup ports directly onto utility classes |
| 20 | Source transformation extension points | **DECIDED** | profile-driven default mapper or thin source adapter → standardized staging → shared source-neutral transforms → canonical; no source logic downstream |
| 21 | Generator outputs | **DECIDED** | Shopify-shaped, Business Central-shaped and external/companion sources, plus source-run manifest and hidden source/causal truth; no canonical publisher |
| 22 | Location keys | **DECIDED** | `locations.location_id` authoritative; demand-only `stores` view uses `store_id = location_id` |
| 23 | Sales/returns semantics | **DECIDED** | explicit cumulative sales versions carry fulfilled units + exact net amount; fulfillment lines bridge demand/supply nodes; pre-fulfilment cancellations are excluded; later unit/revenue reversals are typed, versioned adjustments |
| 24 | Shopify local scope | **DECIDED** | documented adapter + synthetic source projection; exhaustive connector-conformance fixtures are a later tier; real Shopify runs client-controlled |
| 25 | Competitor-match ownership | **DECIDED** | `competitor_matches` is a PoC output; generator match truth is test-only |
| 29 | Partial-source acceptance | **DECIDED** | ingestion-manifest entity/field/capability coverage; pure Shopify may be `validated_partial` only; only capability-complete full Gate-B runs are promoted |
| 30 | Source mapping ownership | **DECIDED** | admins approve immutable mapping config; ingestion writes resolved runtime crosswalks and quarantines unknown/ambiguous keys |
| 31 | ATP and inbound semantics | **DECIDED** | source-observed or fixed bucket formula; on-order and in-transit are disjoint; ATP excludes inbound |
| 32 | Generator configuration | **DECIDED** | Config Builder HTML is the only supported authoring surface; YAML and JSON are losslessly equivalent and contain every resolved run setting |
| 33 | Multi-market topology | **DECIDED** | one retailer scenario may contain multiple explicit markets, stores and warehouses; every node carries market/locale and service relationships |
| 34 | Initial locale packs | **DECIDED** | IN, US, GB and DE; UI may label GB as UK and DE as the PoC European representative; “Europe” is not used as a country code |
| 35 | Missing source metadata | **DECIDED** | source `known_as_of`, versions, manifests, formats and capability declarations are not universal hard requirements; ingestion derives defensible metadata under versioned profile policy or quarantines ambiguity |
| 36 | Datagen isolation | **DECIDED** | `datagen/` imports no downstream `contracts/`, `ingestion/`, `ml/` or `api/` code; ingestion adapts to the published datagen source spec |
| 37 | Generator fidelity tiers | **DECIDED** | exhaustive Shopify lifecycle/HMAC fixtures and full procurement/batch/supplier projections are screen/compliance extensions, not blockers for the first forecast/revenue-pricing round-trip; margin waits for accepted temporal cost |
| 38 | Python environment topology | **DECIDED** | separate `ingestion/` and `ml/` environments/distributions, both depending on the shared `retail-contracts` and `retail-intelligence-execution` packages; `datagen/` remains independently isolated. `contracts/` changes meaning, `execution/` changes only throughput |
| 39 | Guardrail config scoping | **DECIDED** | resolve global dimensionless defaults plus explicit `market_id + currency_code` overrides; absolute money, step/grid and price-ending rules are mandatory per market; Python and Go fingerprint and validate the same resolved policy |
| 40 | Contextual feed scope | **DECIDED** | single-axis calendar/signal/competitor geography uses `market_id + geo_scope_type + geo_scope_id`; region/location identifiers are namespaced within market; unqualified `ALL` is invalid; multi-axis promotion applicability uses explicit qualifier rows |
| 41 | Canonical temporal identity | **DECIDED** | cumulative/correctable facts use explicit monotonic integer versions; observation/reference facts use stable natural key + effective/observation time + `known_as_of`; divergent duplicate complete keys quarantine |
| 42 | Merchandise-scope resolution | **DECIDED** | supplier terms and promotion merchandise targets use `merch_scope_type ∈ {sku, dept, category} + merch_scope_id`, with `sku > dept > category`; supplier origin is exact and nullable only for unmodelled external origin, never a wildcard |
| 43 | Operating vs presentment currency | **DECIDED** | canonical sales/sell prices equal the demand location operating currency; Shopify `shopMoney` is authoritative and `presentmentMoney` is audit/display-only; unsupported mismatches quarantine |
| 44 | FX direction and arithmetic | **DECIDED** | `base_ccy` local → `quote_ccy` reporting at exact `DECIMAL(38,18)` quote-per-base rate; exponent-aware per-fact `ROUND_HALF_EVEN`, then aggregate; shared Python/Go vectors |
| 45 | Pricing evidence demos | **DECIDED** | primary IN+US showcase is sized and tested for ≥25 actually gated series per enabled department in both markets; a separate sparse preset demonstrates reason-coded fail-closed behavior; datagen owns scenario knobs, not ML thresholds |
| 46 | Canonical demand channel grain | **DECIDED** | channel is orthogonal to location; `channel_id` participates in sales, sell-price, assortment and forecast-series grain and is retained on adjustments, fulfillments and planner overrides. One store may serve multiple channels; ingestion never aggregates channel away or invents a separate location solely from channel |
| 47 | Runtime OS portability | **DECIDED** | Windows, macOS and Linux are equal required targets for the Config Builder, contract/code generation, execution resolver, datagen, ingestion, ML, database tooling, Aarv-based Go API, Node/React UI and developer/deployment commands. Authoritative orchestration uses Python + `pathlib`/`tempfile`/argument-list subprocesses; Go uses `filepath` and portable process/lock/shutdown abstractions; UI uses cross-platform npm scripts. POSIX shell/Make are optional wrappers only. Manifests use normalized logical `/` paths while filesystem operations use native paths; UTF-8/LF is explicit for fingerprinted text; open handles are closed before same-volume atomic promotion. Permission/locking uses Windows ACLs/locking or POSIX modes/locking without changing semantics. A layer is not complete until its unit/build/small-fixture checks pass on Windows, macOS and Linux |
| 48 | Go API web framework | **DECIDED** | Use [Aarv](https://github.com/nilshah80/aarv) (`github.com/nilshah80/aarv`) for the API HTTP transport. Pin exact module versions; keep handlers thin and business/read-model logic framework-neutral; keep `contracts/` OpenAPI authoritative; add Aarv plugins à la carte without making framework metadata a second contract |
| 49 | Phase-3 temporal scope | **DECIDED** | Build against the accepted `demand_forecast_non_pit` capability; every artifact declares `pitEligible: false` and `LANDING_BACKFILL_DEPENDENCY`. This does not relax origin/label leakage rules |
| 50 | Forecast label embargo | **DECIDED** | Eight weeks is a fixed floor, independent of the 26-week horizon set; every horizon additionally requires `target_known_as_of_hN <= fit_known_as_of` |
| 51 | Driver presentation groups | **DECIDED** | Merge the auditable origin-observed `event_snap` component into published `weather_local_events`; future calendar-event features remain unavailable with `NO_ORIGIN_VISIBLE_CALENDAR_EVENT_SNAPSHOT`, the separate `local_events` future feed is unavailable with `NO_ORIGIN_VISIBLE_LOCAL_EVENT_PLAN`, and market-disruption futures are unavailable with `NO_ORIGIN_VISIBLE_MARKET_DISRUPTION_PLAN` because their retained rows were published after all historical origins. These unavailable components never enter the model or contribution denominator. Show five live groups, supported by their remaining origin-visible components and renormalized to 100%, plus the fixed unavailable Promotion plan row |
| 52 | Slow-mover acceptance sufficiency | **DECIDED** | Per market: at least 100 eligible slow SeriesKeys, all 13 origins, at least 50 paired series per origin, identical champion/baseline/actual row keys, and positive `actual_sum`; publish series/row/origin counts and a series-clustered bootstrap interval. Otherwise verdict is `insufficient_evidence`, never a pooled substitute |
| 53 | Promotion feature on the accepted pin | **DECIDED** | Do not build a promotion feature: no retained promotion is visible from a historical origin and no future plan exists. Keep the UI row unavailable with reason `NO_ORIGIN_VISIBLE_PROMOTION_PLAN` |
| 54 | Filter-scoped forecast metrics | **DECIDED** | Store additive `abs_error_sum`, `signed_error_sum`, `actual_sum`, `coverage_hits`, `n` at evaluation grain; compose filters by SUM-then-divide. Fixed-slice `forecast_metrics` is a verified convenience, not a competing calculation |
| 55 | External-driver availability | **DECIDED** | `contracts/ml/driver-semantics.yaml` is authoritative: origin-known/history and origin-published target-date values only; explicit missing/fallback state; never target-date actuals or post-origin releases |
| 56 | Partial assortment boundary weeks | **DECIDED** | Retain partial ISO weeks for sales reconciliation with `exposure_days`/`exposure_weight`, but exclude them from model fit, calibration and evaluation. Compute rolling zero share with exposure weights; never treat a partial zero week as seven days of zero demand |
| 12 | Forecast confidence | **DECIDED** | Per row: `relative_spread=max(P90−P50,0)/max(P50,1)` and `confidence=round(1/(1+relative_spread),4)`, clamped to `[0,1]`. Portfolio/slice confidence is the `max(P50,1)`-weighted mean after filters; this defines P50=0 without division by zero |
| 13 | Driver attribution method | **DECIDED** | Deterministic LightGBM TreeSHAP (`pred_contrib=True`) on the P50 model. Aggregate signed features to auditable components, presentation groups and scopes using decision #57; Croston-selected rows carry an explicitly labelled routing explanation rather than fabricated TreeSHAP |
| 57 | Driver aggregation and direction | **DECIDED** | Use P50 SHAP only. Per group magnitude is `SUM(ABS(group_signed))`; live-group contribution is magnitude divided by total live magnitude, rounded to 4 decimals with largest-remainder reconciliation to exactly 100.0000%. Direction is `Mixed` when both positive and negative magnitude shares are at least 20%, otherwise `Up`/`Down` by net sign or `Neutral` at zero. Driver confidence is `MAX(positive_share, negative_share)`. Promotion is excluded; origin-observed `event_snap` remains auditable under `weather_local_events`, while unavailable future calendar, `local_events` and market-disruption values never enter the model |
| 58 | Calibration sufficiency | **DECIDED** | A market+horizon calibration requires at least 100 SeriesKeys, 8 calibration origins, 500 paired rows, identical actual/prediction keys and positive `actual_sum`. Otherwise use the fingerprinted global calibration only when globally sufficient and mark `fallback=true`; an insufficient global calibration leaves the version unaccepted |
| 59 | Forecast version lifecycle | **DECIDED** | Immutable states `candidate → accepted → active → superseded`, with `candidate → rejected` on failed gates. Only a gate-passing accepted version can activate; one active version exists per input/config/market set. Rollback creates a new activation record for an already accepted immutable version; no artifact or verdict is overwritten |
| 61 | Repository CI policy | **DECIDED** | Do not add GitHub Actions or another repository CI workflow now or during later hardening. Authoritative validation remains developer-run; `tools/dev.py verify` is the stateful local phase-exit gate and component commands remain available for focused checks. Portability evidence is collected manually on supported operating-system hosts; decision #47 remains a runtime portability requirement, not a CI requirement. `contracts/validation-policy.yaml` is authoritative and contract validation rejects workflow files |
| 60 | Forecast exception and per-series data-quality policies | **DECIDED** | `contracts/ml/forecast-classification-policy.json` freezes the five current-cycle classes, formulas, thresholds, severity, evidence contract and executable vectors. Under-risk uses P90−P50 gap ≥5 and ratio ≥0.50; over-risk uses P50−MA13 gap ≥5 and ratio ≥0.50; new/sparse is history <26 weeks or zero share >0.60, high at history <13 or zero share >0.80; promotion conflict requires an origin-visible plan and otherwise emits no exception with `NO_ORIGIN_VISIBLE_PROMOTION_PLAN`; data-quality exception means class `Issue`. The quality battery reduces canonical-key, reconciliation, accepted-source findings, 13-week observation coverage, governed core-feature missingness and decision-as-of freshness with `Issue > Watch > Good`. Policy ids and fingerprints are mandatory run identity |
| 62 | Forecast API serving store | **DECIDED** | Parquet/JSON forecast bundles remain the immutable ML authority, but HTTP handlers never scan them. A fail-closed offline materializer first verifies all ten artifacts and their lineage, then atomically loads the API projection into PostgreSQL and records the source run/version/fingerprints. Activation is a separate immutable database record under decision #59; an accepted materialization is not implicitly active. Go queries PostgreSQL through a framework-neutral repository and serves only one lineage-matching active version. Local PostgreSQL and AWS RDS use the same Alembic-owned schema; DuckDB remains an analytical/build tool and is not the forecast API serving authority |
| 63 | Phase 3 local services | **DECIDED** | Bring Docker Compose forward for the Phase 3 serving boundary: Docker Desktop runs pinned PostgreSQL and a shared MLflow server with named volumes, loopback-only ports, health checks and environment-based local credentials. PostgreSQL holds the read-only forecast projection and MLflow metadata; MLflow artifacts use their own volume. Phase 6 extends—not introduces—the stack with workflow/governance tables and API/UI services. Portable `tools/dev.py` commands remain authoritative and no repository CI is added |
| 64 | Demand Forecast parity freeze | **DECIDED** | `contracts/screens/demand-forecast.parity.yaml` is the machine-readable UI authority. Q1–Q18 are frozen: eight completed h1 weekly pairs; current `name, city` store labels; Weekly plus summed Monthly with Daily disabled; category grain; no KPI deltas without a second accepted version; Forecast Health uses decision #80's four exact h1/h4/h8/h13 rows and fingerprinted `retail-forecast-health/v1` while h26 remains diagnostic; selected-horizon workbench sums; `decisionAsOf` freshness; synchronized Store and intersecting Channel filters; MA13 versus active AI; native disabled unavailable controls; 1440×1100 and 390×844 viewports; no currency conversion of units/percentages; governed exception/store/version modals; relative-percent FVA; current-SeriesKey forecast coverage; five live driver contributions plus unavailable promotion; and decision #60 classification |
| 81 | A1 seasonal-naive comparison population | **DECIDED** | Spec §4.3 says the champion beats seasonal naive overall, so acceptance-v2/verifier-v3 require complete champion/actual/seasonal-naive pairing, publish full/paired/dropped champion WAPE and canonical key hashes, and fail A1 when any eligible champion row lacks the baseline. A future partial-pairing floor would be a new pre-result policy decision and cannot grandfather an observed run |

## Finalized from prior recommendations

| # | Decision | Status | Final decision | Spec |
|---|---|---|---|---|
| 6 | Cost valuation method | **DECIDED** | Moving Average Cost is the default; FIFO is allowed only for capabilities with accepted batch/expiry lineage | §10.5 |
| 7 | Who computes WAC | **DECIDED** | Source adapters emit receipt-shaped evidence; canonical ingestion computes `inventory_cost` WAC. Source-provided WAC is reconciliation evidence, not silent authority | §11.2 |
| 8 | Interactive scoring boundary | **DECIDED** | Go computes closed-form projections from stored β/P50/P90; Python owns model-backed scoring and Copilot. Go proxies Python rather than embedding Python/LLM logic | Arch note |
| 9 | Migration ownership | **DECIDED** | Alembic is the sole database-migration owner; Go generates/maintains compatible structs and never runs a competing migration chain | Arch note |

## Finalized Post–Phase 3 decisions

The complete formulas, work-package consumers and evidence requirements remain in
`plans/local/post-phase3-implementation-plan.md` §3.

| # | Decision | Status | Final decision |
|---|---|---|---|
| 65 | Post–Phase 3 scope/order | **DECIDED** | Complete PP3-P0, then fully accept Track A before Track B. Initially authorize PP3-A1–A3 only |
| 66 | Staging migration | **DECIDED** | Introduce immutable `retail-staging/v2`; dual-run v1/v2 and cut over only after complete canonical/Gate-B/capability parity |
| 67 | Role provider resolution | **DECIDED** | Require explicit `exclusive`, disjoint `union`, `cross_validate` or ordered `fallback`; implicit first-source/coalesce behavior is forbidden |
| 68 | Mapped-files language | **DECIDED** | Allow only declarative select/rename/type/value-map/constants/timezone/date/unit operations; no loops, I/O, dynamic SQL or uploaded executable code |
| 69 | Custom-adapter loading | **DECIDED** | Use a static, versioned in-repository registry for the PoC; external/installable plugins are deferred |
| 70 | Temporal evidence/readiness | **DECIDED** | Preserve the five closed evidence grades; business-effective dates never prove availability, and landing-time derivation downgrades capability |
| 71 | Zero-demand eligibility | **DECIDED** | Materialize zero demand only with extract completeness plus dated SKU×location×channel assortment/listing evidence at the cutoff |
| 72 | Capability vocabulary | **DECIDED** | Publish separate `ready`, `validated_partial`, `unavailable`, `insufficient_evidence` and `not_evaluated` outcomes per capability; ingestion success never implies statistical sufficiency |
| 73 | Retailer pin lifecycle | **DECIDED** | Use explicit immutable retailer×tenant×capability×environment selections with `candidate→approved→active→superseded/rejected`; never resolve “latest” |
| 74 | Candidate-selection protocol | **DECIDED** | Use the first 8 chronological scoring origins for development, preregister families, cap the search at 20 configurations and freeze exactly one candidate before reading the final 5 untouched confirmation origins. A1–A5 still runs over all 13 origins |
| 75 | Improvement materiality | **DECIDED** | Require ≥5% relative WAPE improvement versus C0/D0, a seeded SeriesKey-clustered 95% interval whose candidate-minus-authority upper bound is <0, no supported-market relative WAPE regression >1%, identical cohort keys and all acceptance gates. Publish and pass this battery over both all 13 origins and the untouched final 5 |
| 76 | Quality policy v2 | **DECIDED** | Publish global capability limitations separately from row-local `Good/Watch/Issue`; neither may overwrite or hide the other |
| 77 | Business target matrix | **DECIDED** | `retail-forecast-health/v1` freezes exact-horizon accuracy targets at h1/h4/h8/h13/h26: market/portfolio 90/88/85/82/78, store/category 85/82/78/75/70 and SeriesKey 80/78/75/72/68; all use absolute bias ≤5% and P90 coverage 0.85–0.95. Its first-match grain rules and unit conventions are authoritative; insufficient denominators remain unavailable |
| 78 | Presentation policy | **DECIDED** | Preserve the approved HTML layout, label exact grain/horizon, lead with governed contextual evidence, keep weak slices/global limitations visible and never replace SeriesKey quality with an easier aggregate |
| 79 | Provenance vocabulary | **DECIDED** | Keep exact source system/instance separate from closed client/third-party/synthetic and native/derived provenance classes; do not add one shared enum value per retailer |
| 80 | Forecast Health horizon semantics | **DECIDED** | Always show exact h1/h4/h8/h13 additive metrics. `retail-forecast-health/v1` evaluates tiers in Strong→Healthy→Watch→Action order and requires every condition in a tier. `Strong`: accuracy ≥ target+5, \|bias\|≤3%, coverage 0.87–0.93; `Healthy`: accuracy ≥ target, \|bias\|≤5%, coverage 0.85–0.95; `Watch`: accuracy ≥ target−10, \|bias\|≤10%, coverage 0.80–0.98; otherwise `Action`. Any unavailable metric yields unavailable, not a badge |

## Finalized formerly open decisions

No decision remains open in this registry. Vendor/client substitutions remain configuration
choices within the frozen contracts below and do not reopen architectural decisions.

| # | Decision | Status | Final decision | Spec |
|---|---|---|---|---|
| 14 | Competitor data source + SKU↔competitor match key | **DECIDED** | Default to a client-supplied or licensed landed extract/API; scraping is prohibited. The effective-dated match key is tenant×retailer_sku×competitor×competitor_sku with method, confidence, observed_at and known_as_of | §8.1, §11.3 |
| 15 | Production weather granularity + provider | **DECIDED** | Use Open-Meteo for the PoC through a provider-neutral adapter; production may substitute a client-approved provider without downstream change. Prefer geocoded store grain, otherwise market/region, and retain issued_at/observed_at/valid_time. Implementation is not started; the current demo weather is synthesized by datagen and does not call Open-Meteo | §11.5 |
| 18 | Copilot serving | **DECIDED** | Go proxies a separately deployable Python Copilot/model service; both consume the same verified artifacts, and Go remains the auth/audit/API boundary | §8.4 |
| 19 | Customer/segment data depth | **DECIDED** | The PoC accepts aggregate segment counts/mix only. Basket/customer-level PII, cannibalisation and bundle modelling are deferred until a separate privacy-approved scope | §8.1, §11.4 |
| 26 | Incremental ingestion semantics | **DECIDED** | Preserve immutable landed micro-batches; require source-instance watermark plus stable native key, operation, source sequence/version and known_as_of. Deterministically collapse to the latest record at cutoff; late events append and replay, deletes tombstone, and watermark advancement is atomic/idempotent | §11.10 |
| 27 | Tenant reporting-currency accounting policy | **DECIDED** | Use the tenant’s accounting-approved daily closing quote-per-base rate, preferably ERP/treasury supplied; convert by transaction/posting date, fall back only to the most recent prior business-day rate, never a future rate, and permit no accounting-date override without a versioned tenant policy | §2.4, §11.0 |
| 28 | Production Shopify connector scope | **DECIDED** | Client-provided immutable landed exports are the default PoC/production onboarding boundary. A native Shopify API connector is optional later integration work and must emit the same adapter roles without downstream branches | §11.11 |
| 82 | Short-history A1 comparator and evaluation eligibility | **DECIDED** | Established rows require origin-visible lag-52 and 100% seasonal-naive pairing. All other rows enter a cold-start cohort using `mean(last min(13, history_weeks) complete origin-visible weeks)` with at least one prior week; any zero-history row is `insufficient_evidence` and leaves the version unaccepted. Cold-start champion WAPE must be ≤ its comparator globally and per market with complete keys. A3 remains a seasonal-naive gate over established-history slow movers only; cold-start slow movers remain in the cold-start A1 gate and never fabricate a seasonal comparator. Publish acceptance-v3/verifier-v4 and migration 0006 as v4-only; v2/v3/0005 remain immutable and ineligible for new activation | §4.3 |

## Reserved — pre-result proposal

| # | Decision | Status | Topic |
|---|---|---|---|
| 83 | Launch-week cold-start comparator eligibility | **DECIDED** | Decision #82's "complete origin-visible weeks" leaves 54 measured `forecast_origin × SeriesKey` pairs (1,404 rows) with one partial prior week and no complete week, so the cold-start gate returns `insufficient_evidence` for every rolling-origin panel containing a series' launch week and no version can ever be accepted. **Option (a) is decided**: when a cold-start row has no complete origin-visible prior week, admit the exposure-normalised `weekly_units_equivalent` of its most recent partial origin-visible week. The feature contract already computes that quantity for exactly this purpose, so the comparator stays real observed demand rather than a fabricated value. Option (b) is rejected: declaring launch-week rows evaluation-ineligible creates a third population, which #82's "a row may not disappear from both cohorts" forbids. A row with no prior observation of **any** kind at its first origin is `evaluation_ineligible` with reason code `NO_PRIOR_OBSERVATION_AT_FIRST_ORIGIN`, published with a count and share, and capped at 1% of rows — a principled ceiling above which the residue is a systemic evidence problem and must fail closed, not a launch-week edge. Measured residue is 0.0587% of rows (416 rows, 16 SeriesKeys). This is explicit and counted, so it is not the silent disappearance #82 forbids. Decision #82 is otherwise unchanged. This is **not** tuning to pass: the US New York cold-start deficit was measured at −3.07% before this choice and is unaffected by it, so acceptance remains blocked on model quality. See `plans/local/post-phase3-implementation-plan.md` §3.1.1 |

| 84 | Cold-start estimator policy | **DECIDED** | The sole failing gate is `us-new-york x cold_start` A1 non-inferiority at -2.399%, a cohort carrying 17.053% of global absolute error on 12.93% of volume. Measured mechanism: the champion beats the comparator on 54.0% of rows yet loses on WAPE, is under-dispersed (std 93.66 against an actual 132.54, comparator 118.50) and more under-biased (-23.50% against -15.00%). The deficit is entirely long-horizon - the champion wins h1-h4 at -15.0% relative and loses h14-h26 at +9.0%, and that band carries 56.0% of the cohort's error - which corroborates supported cause H7 and PP3-B6's 96.96% climatology fallback. **Proposal:** blend cold-start rows only, `p50 = w * lgbm_p50 + (1-w) * cold_start_mean`, `w` fitted per market x exact horizon on the 8 development origins from a frozen 21-point grid, shrunk to parent on C3's frozen sufficiency rule, P90 keeping the champion's absolute interval width. Acceptance requires all of A1-A5, cold-start non-inferiority in both markets, byte-identical established-cohort predictions, A2 coverage inside 0.85-0.95, and decision #75's full battery passing on both all-13 and final-5 including the 5% global floor - which is the binding bar, since non-inferiority alone needs only 0.400% of global error shed against a 17.053% ceiling. Five prior candidates (C1, C2, C1+C2, C3, C4) all rescaled or re-intervalled rather than replacing the estimator. **Reviewed and approved 2026-07-31 with `cold_start_mean` retained as the blend target**, the `ma13` cosmetic-separation alternative declined. The stated objection stands on the record: the blend target is the gate's own yardstick, bounded by #75's independent 5% global floor, which is the criterion that must carry acceptance rather than non-inferiority. See `docs/decision-84-cold-start-estimator-proposal.md` |

| 85 | Per-cohort P90 coverage | **DECIDED — report-only until Phase 4 entry** | Decision #82 cohorted A1 but left A2 whole-population, so a genuinely under-covered cohort is invisible to the gate. Measured on `fr_a5b88c2ef23091ee`, A2 reports 0.8887 passing globally while **every cold-start scope sits below the 0.85 floor**: 0.7937 global, 0.7785 us-new-york, 0.8114 india-west. The established cohort carries the pooled number. Same class of defect as A5 - a failure hidden by aggregation. **A2 is now evaluated per cohort** (`established_history` and `cold_start`; `evaluation_ineligible` is unscored) globally and per supported market, using the **unchanged 0.85-0.95 band** - choosing a looser floor for the cohort that fails would be tuning against a visible result. A zero-actual or decision-#52-insufficient cell is `insufficient_evidence`, never a pass. The whole-population figure is still published but no longer decides the gate alone; every scored cell must be inside the band. **This adds a second Phase 3 blocker immediately** and that is the intended behaviour, on the precedent that #82 invalidated three self-declared accepted runs and #83 a fourth. #84's C5 preserves the champion's absolute interval width while raising the centre, so coverage should move up, but overshooting past 0.95 is a real risk and #84's stop rules forbid adjusting intervals to rescue either direction. **Amended the same day to report-only for the current version**, with a hard-gate deadline at **Phase 4 entry** - a dependency rather than a date, because Phase 4 safety stock is quantile-spread x service level, so an under-covered P90 feeds an under-stocked reorder point. A phased introduction, not a repeal: no version was ever evaluated against this gate, so nothing that passed is excused. Every cell is still computed and published with `coverageGateMode: report_only` and the deadline. **Correction: only the acceptance id moved.** Recomputation and verifier were already v4 from #82, so the promised fail-closed version boundary was never created and migration 0006 still admits verifier-v4 materialisations. Tolerable only while the gate is report-only; creating the boundary is a precondition of making it hard at Phase 4 entry. See `docs/decision-85-per-cohort-coverage.md` |

| 86 | Gate-remediation candidate class | **DECIDED** | Decision #75's 5% global floor was written to stop victory being declared from a cherry-picked slice and is correct for that, but it is the wrong instrument for a candidate that repairs a failing gate without claiming a global gain: the cold-start cohort is 22.5% of volume, so a 5% global move demands ~22% cohort improvement against an actual requirement of not losing to a mean of recent demand. This class separates the two claims. A remediation candidate must name the failing gate and scope before scoring, pass that gate's criterion in **every** scope, leave untargeted populations **byte-identical**, break no decision #77 display cell (no pass-to-fail, no regression above 0.1pp display rounding), keep the leakage battery clean, still **publish** #75's full battery, and may regress a report-only metric only when already outside band, by <=2pp, published, with a hard-gate deadline recorded. It is **forbidden** from being presented as an accuracy improvement anywhere, and the acceptance document must carry `candidateClass: gate_remediation`. **C5 adopted**: us-new-york cold-start -2.399% -> +4.446%, all scopes pass, 605,904 established rows byte-identical, #75 published at +1.754% and adopted anyway. **The framing order was deliberately violated - C5's numbers were visible when these criteria were written - and that is recorded as an instructed exception in the decision itself rather than presented as equivalent rigour.** Decision #75 is unchanged. See `docs/decision-86-remediation-candidate-class.md` |

| 87 | Phase 4 cold-start interval hard-gate remedy | **RESERVED — not yet framed or decided** | Reserved for the pre-result P4-D1 protocol. Reservation approves no candidate family, configuration, threshold, or result; the decision must be framed and frozen before fitting. |

| 88 | Neutral `location` role field names | **OPEN — needs decision** | Found while proving a mapped-files-only run through the real `build_staging()` entrypoint. `contracts/staging/staging-v2.yaml` freezes the `location` role's required fields as `location_source_key`, `name`, `location_kind`. The Shopify adapter has always emitted `name AS location_name` and `upper(locationType) AS location_type` (`ingestion/src/retail_ingestion/adapters/shopify.py:175-176`), and `build_location_crosswalk` reads those dialect spellings. So the neutral `locations` relation has never presented the field names its own frozen contract declares, and PP3-A3's "built from the neutral role, not a platform relation" is true only of the relation's *name* — its columns are still Shopify-shaped. A retailer whose adapter conforms to the contract produced columns the crosswalk could not find. **Interim measure, not the decision:** the crosswalk now accepts either spelling, so both the generator path and a conforming retailer work, and the divergence is visible rather than latent. **Options.** (a) Change the Shopify adapter to emit the contract's names and update the crosswalk and any other consumer — correct, but it edits the working generator path and its blast radius must be measured before Phase 4. (b) Amend staging-v2 to declare `location_name`/`location_type` — cheaper, but PP3-A2 froze that contract and it is fingerprinted, so this is a contract amendment with an identity change, and it also blesses a dialect-flavoured name in a source-neutral contract. (a) is recommended: the whole point of the neutral role is that no retailer has to know how Shopify spells a column. Not a Phase 3 blocker — no accepted-run number depends on it — but it should not reach Phase 4 unresolved, because every new adapter written against the frozen contract will hit it. |

| 89 | Snapshot identity is the only fingerprint that is not layout-invariant | **OPEN — needs decision** | Found by the authorized clean-slate rebuild on 2026-07-31, the first regeneration of the pinned run from an empty tree. **The data reproduced; the identity chain did not.** Datagen returned `run-c5eb1506ecd4c550` with `reused: false`, and every independently committed exact-value assertion in `ingestion/tests/test_pinned_run.py` passed against the regenerated run: both currencies' gross/net/tax to the cent, orders, units, `fillRate 0.976855`, 8,800,322 orders, 15,785,727 order lines, 8,726 objects, 252,864,055 source-truth rows, Gate A inventory 132, Gate B B03/B15/B21 evidence exact, both gates `pass`. Yet `sourceSnapshotId` moved `681090ee… -> e010c549…`, Gate A, Gate B and the publication fingerprint moved with it, and `expected-pin.json` fail-closed the ML stages. **Root cause, proven not inferred.** `publication-manifest.json`'s fingerprint deliberately excludes physical layout: `publisher.py:432` passes `volatile_pointers=("/duckdb", "/objects", "/publishedAt", "/executionProfile")`. I verified that exclusion works by republishing the same candidate database under `performance` instead of `safe`: object count changed 2,069 -> 2,052 while the publication fingerprint stayed byte-identical at `fa1bf883…`. So the publication fingerprint moved for exactly one reason — its payload carries `sourceSnapshotId` and `gateBSemanticFingerprint`, and those inherited the drift. And `source_snapshot_id` (`landing/snapshot_id.py`) hashes `(object_path, logical_path, bytes, sha256)` per object **with no volatile-pointer exclusion at all**. Parquet `bytes`/`sha256` encode physical layout, and datagen layout is profile-dependent (`safe` marketWorkers 1 / partitionWorkers 4 versus `performance` 2 / 8). **So the one identity at the base of the chain is the only one that is not layout-invariant, and every downstream fingerprint inherits its instability.** `contracts/README.md` already scopes byte-identical Parquet to "a fully pinned writer and execution profile", which is consistent with this — but the snapshot identity carries no such qualification while presenting itself as a content identity. Secondary, independent defect: `source-run-manifest.json` embeds `executionTelemetry` (`cpuProcessSeconds`, `cpuUtilizationPct`, elapsed, per-worker `wallSeconds`/`peakRssBytes`), so its committed SHA-256 in `test_pinned_run.py` cannot be satisfied by any regeneration on any host. `runIdentityMethod` already excludes telemetry from `runId`; that hash does not. **Options.** (a) Give `source_snapshot_id` a logical content digest — schema plus ordered row digests — so equivalent data yields one identity regardless of layout. Correct, and the most work; a row digest over 252M rows needs a cost check. (b) Keep byte identity but make its scope honest: exclude telemetry from the manifest hash, and pin the writer and execution profile as part of the pinned run, so the identity is reproducible *under the pin* and documented as nothing more. (c) Accept it as an exact-artifact pin and rely on the exact-value assertions as the real reproducibility guard. (b) now, (a) before any client onboarding — a client who regenerates an extract must not be told their data changed when the control totals prove it did not. **Interim:** the pin was re-established from the new run with the equivalence evidence recorded inline under `repinned` and the old pin kept as `supersededPin`; the manifest hash in `test_pinned_run.py` was re-pinned with its reasoning in place, and every exact-value assertion left untouched. No number was adjusted to make anything pass. |

| 90 | Forecast activation authority scope and supersession | **OPEN — needs decision** | PostgreSQL currently exposes two active forecasts over the same input bundle, feature fingerprint and markets: `fr_9aaa1d4431381570` / `fv_efcbbc03d991007f` and `fr_422e1113355eb05b` / `fv_86cd60c37be7e203`; both activation events have `prior_event_id = NULL`, and the former bundle bytes are gone. `_activation_scope` hashes `inputBundle`, `featureSemanticFingerprint`, `modelPolicy`, `classificationPolicies` and markets, so the refitted 52-weight model policy minted a parallel active scope. Go then selects only the configured `activation_scope_fingerprint` and cannot detect the competing authority. This violates decisions #59 and #62 despite each hash scope having one row. **Options.** (a) Recommended: define one authority scope by input bundle + feature fingerprint + markets (and retailer/tenant/environment when carried), keep model/classification policy fingerprints in run lineage, require every new activation to append a supersession chain with `prior_event_id`, and fail closed unless exactly one active row exists across all policy/config fingerprints for that authority scope. (b) Amend #59 to allow parallel model-policy scopes and define a deterministic, governed winner in PostgreSQL and Go. No P4-1 model-policy publication or activation may begin until this is decided; otherwise it can mint a third active scope. |

## Do NOT carry over from the M5 PoC

- The M5-only `M5_POC_DEMONSTRATION_V1` pricing amendment (widened resample-IQR gate). A real
  retailer starts on the strict Plan v3 gates; any amendment must be separately justified,
  versioned, approved, and disclosed. (§4.6)
- Synthetic cost presented as real. A real margin objective requires non-synthetic,
  provenance-matched cost; generated cost = *labelled* margin scenario only. (§10.3)
